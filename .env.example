# =============================================================================
# AI-POWERED SDLC AUTOMATION SYSTEM - ENVIRONMENT VARIABLES
# Copy this file to .env and fill in your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# GOOGLE CLOUD PLATFORM (Required for Gemini AI)
# -----------------------------------------------------------------------------
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1
VERTEX_AI_LOCATION=us-central1

# -----------------------------------------------------------------------------
# GOOGLE GEMINI AI (Required - Real API)
# -----------------------------------------------------------------------------
# Option 1: Use API Key (easier for testing)
GEMINI_API_KEY=your-gemini-api-key-here
# Option 2: Use GCP Application Default Credentials (leave GEMINI_API_KEY empty)
# Run: gcloud auth application-default login

GEMINI_MODEL=gemini-2.5-pro
GEMINI_MAX_TOKENS=8192
GEMINI_TEMPERATURE=0.2

# -----------------------------------------------------------------------------
# MOCK MODE CONFIGURATION
# Set MOCK_MODE=true to use mock data for testing
# When MOCK_MODE=true, only Gemini AI credentials are required
# -----------------------------------------------------------------------------
MOCK_MODE=true

# -----------------------------------------------------------------------------
# AZURE DEVOPS API (Only required if MOCK_MODE=false)
# -----------------------------------------------------------------------------
ADO_ORGANIZATION=mock-org
ADO_PROJECT=mock-project
ADO_BASE_URL=https://dev.azure.com
ADO_API_VERSION=7.0
ADO_WEBHOOK_SECRET=mock-webhook-secret

# -----------------------------------------------------------------------------
# FIGMA API (Only required if MOCK_MODE=false)
# -----------------------------------------------------------------------------
FIGMA_BASE_URL=https://api.figma.com/v1

# -----------------------------------------------------------------------------
# GITHUB API (Only required if MOCK_MODE=false)
# -----------------------------------------------------------------------------
GITHUB_BASE_URL=https://api.github.com
GITHUB_APP_ID=mock-app-id
GITHUB_INSTALLATION_ID=mock-installation-id
GITHUB_WEBHOOK_SECRET=mock-webhook-secret

# -----------------------------------------------------------------------------
# CLOUD SERVICES (Only required if MOCK_MODE=false)
# -----------------------------------------------------------------------------
CLOUD_RUN_SERVICE_URL=https://mock-service-url.run.app
PUBSUB_TOPIC_ADO_EVENTS=ado-story-events
PUBSUB_TOPIC_GITHUB_EVENTS=github-pr-events
PUBSUB_TOPIC_SECURITY_ALERTS=security-alerts
CLOUD_STORAGE_BUCKET=mock-storage-bucket

# -----------------------------------------------------------------------------
# SECRET MANAGER REFERENCES (Only required if MOCK_MODE=false)
# -----------------------------------------------------------------------------
ADO_PAT_SECRET=projects/your-project/secrets/ado-pat/versions/latest
FIGMA_TOKEN_SECRET=projects/your-project/secrets/figma-token/versions/latest
GITHUB_APP_KEY_SECRET=projects/your-project/secrets/github-app-key/versions/latest

# -----------------------------------------------------------------------------
# AGENT CONFIGURATION
# -----------------------------------------------------------------------------
MAX_RETRY_ATTEMPTS=5
AGENT_TIMEOUT_MINUTES=30
SELF_HEALING_MAX_ATTEMPTS=5
FEEDBACK_LOOP_MAX_CYCLES=10

# -----------------------------------------------------------------------------
# CODE GENERATION SETTINGS
# -----------------------------------------------------------------------------
TARGET_LANGUAGE=typescript
FRONTEND_FRAMEWORK=react
BACKEND_FRAMEWORK=nodejs
TEST_FRAMEWORK=jest
PACKAGE_MANAGER=npm

# -----------------------------------------------------------------------------
# VALIDATION SETTINGS
# -----------------------------------------------------------------------------
TYPESCRIPT_STRICT_MODE=true
ESLINT_CONFIG=@typescript-eslint/recommended
PRETTIER_CONFIG=standard
CODE_COVERAGE_THRESHOLD=80

# -----------------------------------------------------------------------------
# SECURITY SETTINGS
# -----------------------------------------------------------------------------
SECURITY_SCAN_ENABLED=true
VULNERABILITY_CHECK_INTERVAL=3600
SECURITY_ALERT_WEBHOOK=https://mock-security-webhook.com

# -----------------------------------------------------------------------------
# LOGGING & MONITORING
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
ENABLE_STRUCTURED_LOGGING=true

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS
# -----------------------------------------------------------------------------
ENVIRONMENT=development
DEBUG_MODE=true
TEMP_WORKSPACE_PATH=/tmp/ai-sdlc-workspace

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
ENABLE_AUTO_MERGE=false
ENABLE_SECURITY_AUTO_FIX=true
ENABLE_FEEDBACK_LOOP=true
ENABLE_SELF_HEALING=true

# =============================================================================
# MOCK MODE TESTING INSTRUCTIONS
# =============================================================================
# 
# For testing with mock data:
# 1. Set MOCK_MODE=true
# 2. Only configure GCP_PROJECT_ID and Gemini AI credentials
# 3. All other APIs (Azure DevOps, Figma, GitHub) will use mock data
# 4. Test story ID: 12345 (predefined mock story)
# 5. Test endpoint: POST /api/v1/process-story {"story_id": 12345}
#
# The system will:
# - Use real Gemini AI for code generation (with your credentials)
# - Use mock Azure DevOps story data
# - Use mock Figma design data  
# - Use mock GitHub repository data
# - Generate real TypeScript code using AI
# - Simulate GitHub operations (branch, commit, PR creation)
#
# =============================================================================